# Groq API key (required — free at https://console.groq.com, no credit card needed)
GROQ_API_KEY=gsk_your-key-here
# OpenAI API key (optional — only needed if you switch embeddings to text-embedding-3-small)
OPENAI_API_KEY=sk-...
